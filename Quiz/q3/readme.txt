1. file One.m contains the program

2. file Two.m contains the program

3. file Three.m contains the program

4. 
  The first difference results from the shortcircuit or '||' which stops the comparison once a true value is found.  For example,
  true || false, only runs through the first true statement, ignoring the false entirely.
  The second difference is that the '||' operator can only apply to scalar values, whereas, the '|' operator can be applied to both
  scalar and vector values.
  
5.
